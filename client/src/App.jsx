import React from 'react';
import { AppShell } from './layout/AppShell';

// Remove the old App.css import — all styling goes through Tailwind via index.css
function App() {
  return <AppShell />;
}

export default App;